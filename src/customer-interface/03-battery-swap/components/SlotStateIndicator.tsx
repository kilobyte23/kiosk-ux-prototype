/**
 * Slot state indicator: green (available), amber (charging), blue (reserved), red (fault).
 */
export default function SlotStateIndicator() {
  return (
    <div className="SlotStateIndicator">
      <h2>SlotStateIndicator</h2>
      <p>Slot state indicator: green (available), amber (charging), blue (reserved), red (fault).</p>
    </div>
  );
}
