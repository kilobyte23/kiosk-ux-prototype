/**
 * Swap confirmation: old battery SoC → new battery SoC, estimated range gain.
 */
export default function SwapConfirmationScreen() {
  return (
    <div className="SwapConfirmationScreen">
      <h2>SwapConfirmationScreen</h2>
      <p>Swap confirmation: old battery SoC → new battery SoC, estimated range gain.</p>
    </div>
  );
}
