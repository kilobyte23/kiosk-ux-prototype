/**
 * Swap timer countdown — starts after door unlock, auto-cancel on timeout.
 */
export default function SwapTimerScreen() {
  return (
    <div className="SwapTimerScreen">
      <h2>SwapTimerScreen</h2>
      <p>Swap timer countdown — starts after door unlock, auto-cancel on timeout.</p>
    </div>
  );
}
