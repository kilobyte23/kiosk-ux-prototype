/**
 * Scan success / failure visual feedback.
 */
export default function ScanFeedback() {
  return (
    <div className="ScanFeedback">
      <h2>ScanFeedback</h2>
      <p>Scan success / failure visual feedback.</p>
    </div>
  );
}
