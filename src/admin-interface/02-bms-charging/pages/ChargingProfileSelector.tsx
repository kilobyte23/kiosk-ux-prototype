/**
 * Per-slot charging profile selector: CC/CV, fast charge, trickle charge.
 */
export default function ChargingProfileSelector() {
  return (
    <div className="ChargingProfileSelector">
      <h2>ChargingProfileSelector</h2>
      <p>Per-slot charging profile selector: CC/CV, fast charge, trickle charge.</p>
    </div>
  );
}
