/**
 * Fault code decoder: raw BMS codes mapped to readable descriptions.
 */
export default function FaultCodeDecoder() {
  return (
    <div className="FaultCodeDecoder">
      <h2>FaultCodeDecoder</h2>
      <p>Fault code decoder: raw BMS codes mapped to readable descriptions.</p>
    </div>
  );
}
